{
  "name": "hardproof-rebuild",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "clean": "rm -rf packages/*/dist apps/*/dist",
    "build": "npm run build -w @hardproof/crypto && npm run build -w @hardproof/core && npm run build -w @hardproof/simulator && npm run build -w @hardproof/prover-tpm && npm run build -w @hardproof/sdk && npm run build -w @hardproof/middleware && npm run build -w @hardproof/cli && npm run build -w @hardproof/cloud",
    "typecheck": "npm run build",
    "test": "node --test tests/*.test.mjs",
    "demo": "node packages/cli/dist/index.js demo"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
